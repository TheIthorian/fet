import UserConfig from 'App/Models/UserConfig';
import Factory from '@ioc:Adonis/Lucid/Factory';

export default Factory.define(UserConfig, ({ faker }) => {
    return {
        //
    };
}).build();
